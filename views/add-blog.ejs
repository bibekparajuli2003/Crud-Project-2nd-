<!DOCTYPE html>
<html>
<head>
  <title>Registration Form</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #efe8e8;
      margin: 0;
      padding: 0;
    }
    
    .container {
      max-width: 400px;
      margin-left: 40%;
			margin-top: 5%;
      padding: 20px;
      background-color: #e3dede;
      border-radius: 5px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.3);
     
    }
    
    h2 {
      text-align: center;
    }

    form{
      padding-top: 5px;
    }

    input[type="text"], input[type="email"], input[type="password"] {
      width: 100%;
	  padding: 10px 20px;
      margin: 15px 0;
      /* margin-bottom: 20px; */
      border: 1px solid #ccc;
      border-radius: 4px;
      box-sizing: border-box;
      /* display: inline-block; */
    }
    .choose-img{
        padding-top: 10px;
    }
    
    input[type="submit"] {
      width: 100%;
      background-color: #535653;
      color: #fff;
      padding: 10px;
      border: none;
      border-radius: 4px;
      cursor: pointer;
      margin: 15px 0;
    }
    
    input[type="submit"]:hover {
      background-color: #cae9cc;
    }

/* select file option */
.choose-img{
    display: flex;
    align-items: center;
    /* font-size: 16px; */
}

.choose-img label{
    margin-right: 10px;
} 
/* .choose-img input{
    /* display: none; */


.choose-img label,
.choose-img input[type="file"] + label{
    background-color: #fff;
    padding: 10px 20px;
    border-radius: 4px;
    color: #555;
    cursor: pointer;
}
/* desc */
.desc-field {
      display: flex;
      flex-direction: column;
      font-family: Arial, sans-serif;
      font-size: 16px;
    }
    
    .desc-field label {
        padding-top: 20px;
      margin-bottom: 5px;
    }
    
    .desc-field textarea {
      resize: vertical;
      padding: 10px;
      border: 1px solid #ccc;
      border-radius: 4px;
      font-family: Arial, sans-serif;
      font-size: 16px;
    }
  
  </style>
</head>
<body>


  <div class="container">
    <h2>Blog Form</h2>
    <form action="/createBlog" method="post">
      <label for="title">Title</label>
    <input type="text" id="title" name="title" placeholder="Enter the blog title" required>

      <div class="csub">
        <label for="subtitle">Subtitle:</label>
    <input type="text" id="subtitle" name="subtitle" placeholder="Enter the blog subtitle" required>
      </div>
      <div class="desc-field">
        <label for="description">Description.</label>
        <textarea id="description" name="description" rows="5"></textarea>
      </div>
      <input type="submit" value="Add Blog">
    </form>
  </div>
</body>
</html>
